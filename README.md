# openclaw-skill

A small repository of OpenClaw skills for local automation.

## Contents

- `skills/ocado-android/` — Ocado Android shopping helper script (ADB + uiautomator2)
- `skills/ocado-cli/` — CLI wrapper that exposes a friendlier command surface over the same automation

Both variants are intentionally local-only and include no third-party service credentials.
